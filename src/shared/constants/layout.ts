/**
 * Shared layout constants
 * Used for resolution-independent scaling of UI elements (padding, corners, cursor).
 */

// The reference resolution for which "1px" of UI value (padding/radius) is designed.
// If the composition is 4K, values are scaled up by 2x.
export const REFERENCE_WIDTH = 1920;
export const REFERENCE_HEIGHT = 1080;
